import StudentList from "../components/StudentList";

const StudentListPage = () => { 
    return ( 
    <section> 
    <h1>Student List Page</h1>
    <StudentList />
    </section> 
    ); 
    }; 
    export default StudentListPage; //Having a default, don't need to use culry braces when importing